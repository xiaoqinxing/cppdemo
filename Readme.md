## 脚本

```bash
./build.sh clean 清理缓存

./build.sh sync 更新代码

./build.sh opencv 预编译 opencv(第一次下载代码需要编译下)

./build.sh target(e.g. all) --install(optional) Release(optional default:Debug) 编译模块
例如全量编译：
./build.sh all
```

## 模块
